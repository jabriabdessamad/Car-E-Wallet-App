import 'package:flutter/material.dart';

class RestaurantMore extends StatefulWidget {
  const RestaurantMore({Key? key}) : super(key: key);

  @override
  State<RestaurantMore> createState() => _RestaurantMoreState();
}

class _RestaurantMoreState extends State<RestaurantMore> {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('RestaurantMore'));
  }
}
